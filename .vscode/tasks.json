{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PlatformIO: Upload OTA (esp32_ota)",
            "type": "shell",
            "command": "platformio run --target upload --environment esp32_ota",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            },
            "problemMatcher": []
        }
    ]
}
